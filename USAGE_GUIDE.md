# 🌀 Poincaré Streamlit Web 应用 - 使用指南

## 📋 功能概览

该 Streamlit 应用提供了三个主要功能模块：

### 1️⃣ 基础镶嵌生成（Tessellation Generator）

**功能描述**：
- 生成参数化的双曲镶嵌
- 支持实时参数调整和预览
- 可导出 STL 3D 模型

**参数说明**：

| 参数             | 范围             | 说明                       |
| ---------------- | ---------------- | -------------------------- |
| **镶嵌类型**     | hex, tri, square | 选择不同的镶嵌类型         |
| **p (边数)**     | 3-10             | 多边形的边数               |
| **q (顶点个数)** | 3-10             | 每个顶点处相聚的多边形个数 |
| **k (深度)**     | 1-10             | 递归深度（越大越复杂）     |
| **ring_inrad**   | 0.5-1.0          | 外环内半径                 |
| **phi (旋转)**   | 0-2π             | 初始多边形旋转角度         |
| **fill_color**   | True/False       | 是否填充颜色               |
| **border_size**  | 0.005-0.05       | 边框粗细                   |

**使用流程**：
1. 选择"基础镶嵌生成"选项卡
2. 在左侧调整参数
3. 右侧实时显示预览
4. 查看多边形数量和统计信息
5. 可选：生成并下载 STL 模型

**推荐参数配置**：

```
高清晰度 (工作站):
- k = 8-10
- 边框大小 = 0.02

平衡配置:
- k = 6
- 边框大小 = 0.015

快速生成:
- k = 3-4
- 边框大小 = 0.025
```

---

### 2️⃣ 文字投影（Text Projection）

**功能描述**：
- 将输入的中文文字投影到双曲空间
- 创建独特的双曲文字艺术
- 支持各种文字缩放和位置调整

**参数说明**：

| 参数         | 范围     | 说明             |
| ------------ | -------- | ---------------- |
| **输入文字** | 最多20字 | 要投影的文字内容 |
| **缩放倍数** | 0.5-3.0  | 文字相对大小     |
| **垂直偏移** | -1.0-1.0 | 文字的竖直位置   |
| **字符间距** | 0.05-0.3 | 字符之间的距离   |

**使用流程**：
1. 选择"文字投影"选项卡
2. 在输入框中输入要投影的文字
3. 调整缩放、偏移和间距参数
4. 右侧实时显示投影效果
5. 标准镶嵌背景（单位圆）

**创意用途**：
- 生成艺术字体
- 制作海报或设计素材
- 创建数学相关的视觉作品

**文字支持**：
- ✅ 中文（简体/繁体）
- ✅ 英文字母
- ✅ 数字
- ✅ 常见符号

**示例文字**：
```
你好
Hello
数学的天空
Poincaré
π≈3.14
```

---

### 3️⃣ 图片处理（Image Processing）

**功能描述**：
- 上传图片进行边界检测
- 提取线条特征
- 投影到双曲空间（开发中）

**参数说明**：

| 参数         | 范围        | 说明                 |
| ------------ | ----------- | -------------------- |
| **图片**     | jpg/png/bmp | 上传的原始图片       |
| **边界阈值** | 50-200      | Canny 边界检测的阈值 |
| **最小线条** | 5-50        | 过滤过短线条的长度   |
| **缩放因子** | 0.2-2.0     | 投影大小             |

**使用流程**：
1. 选择"图片处理"选项卡
2. 上传图片文件
3. 调整边界检测参数
4. 查看边界检测结果
5. （开发中）投影到双曲空间

**支持格式**：
- JPEG/JPG (.jpg, .jpeg)
- PNG (.png)
- BMP (.bmp)

**建议**：
- 上传清晰的黑白或高对比图片
- 图片尺寸不超过 2000×2000 像素
- 线条简洁的图案效果最佳

---

## 🎯 快速开始

### 第一次运行

```powershell
# 1. 导航到项目目录
cd "c:\Users\21140\Desktop\数学的天空网站\poincare"

# 2. 安装依赖（如果还未安装）
pip install -r requirements.txt

# 3. 启动应用
streamlit run app.py

# 或双击运行脚本
.\run.ps1      # PowerShell
run.bat        # 命令提示符
```

### 应用将自动打开

浏览器会自动打开 `http://localhost:8501`

---

## 🔧 高级用法

### 导出结果

**导出 PNG 图像**：
- 使用浏览器的截图工具 (F12 → 截图)
- 或在 Streamlit 菜单中选择"Download as PNG"

**导出 STL 模型**：
- 在"基础镶嵌生成"中勾选"生成 STL 模型"
- 点击"📥 下载 STL 模型"按钮
- 可用于 3D 打印软件处理

### 修改参数范围

编辑 `app.py`，查找 `st.slider()` 函数，修改范围：

```python
# 例如修改 k 的范围
k = st.slider("k (递归深度)", 1, 15, cfg['k'])  # 改为 1-15
```

### 自定义配置

编辑 `.streamlit/config.toml` 修改主题和界面设置：

```toml
[theme]
primaryColor = "#FF6B6B"        # 主色调
backgroundColor = "#FFFFFF"     # 背景色
secondaryBackgroundColor = "#F0F2F6"  # 次背景色
textColor = "#262730"           # 文字颜色
font = "sans serif"             # 字体

[server]
port = 8501                     # 端口（可改）
```

---

## 📊 参数建议

### 按性能分类

**超高精度** (需要较强 CPU/GPU，运行时间 30-60 秒):
```
p=7, q=3, k=10, border_size=0.01
```

**高精度** (10-30 秒):
```
p=7, q=3, k=8, border_size=0.015
```

**标准质量** (3-10 秒):
```
p=7, q=3, k=6, border_size=0.02
```

**快速预览** (< 3 秒):
```
p=7, q=3, k=4, border_size=0.025
```

### 按镶嵌类型

**六边形 (hex) - {7, 3}**:
- 视觉效果最佳
- 推荐 k=6-8
- 最常用

**三角形 (tri) - {3, 7}**:
- 更复杂的图案
- 推荐 k=8-10
- 计算密集

**正方形 (square) - {4, 5}**:
- 中等复杂度
- 推荐 k=6-7
- 平衡选择

---

## 🐛 常见问题

### Q: 应用启动很慢？
**A**: 这是正常的。Streamlit 首次启动需要：
1. 编译代码
2. 加载所有库
3. 构建前端

首次启动可能需要 30-60 秒。之后的重载会快很多。

### Q: 参数调整后没有反应？
**A**: 
- 确保您修改的是左侧边栏的参数
- Streamlit 会自动检测参数变化并重新运行
- 查看右下角的"重新运行"提示

### Q: 生成 STL 失败怎么办？
**A**: 
```powershell
pip install --upgrade trimesh numpy-stl
```

### Q: 文字投影显示不全或变形？
**A**: 
- 尝试调整"缩放倍数"参数
- 减少输入文字数量（建议 2-4 字）
- 调整"垂直偏移"

### Q: 程序占用太多内存？
**A**: 
- 降低 k 值（递归深度）
- 减小图片尺寸
- 关闭其他应用释放内存

### Q: 能否在云端部署？
**A**: 是的！可以部署到：
- Streamlit Cloud (推荐)
- Heroku
- AWS/Azure
- 自建服务器

---

## 📚 数学概念简介

### Poincaré 圆盘

Poincaré 圆盘是欧氏空间到双曲空间的一个经典映射：
- 单位圆内的所有点代表整个双曲平面
- 距离边界越近，相对距离越大
- 这产生了独特的"透视"效果

### 双曲镶嵌 {p, q}

- `p` = 多边形边数
- `q` = 顶点周围的多边形数量

例：{7, 3} 表示七边形镶嵌，每个顶点 3 个七边形相交

在欧氏几何中，只有 {3,6}、{4,4}、{6,3} 可行。
在双曲几何中，无限多个 {p, q} 组合都可行！

### 反演（Inversion）

反演是这个程序的核心算法：
1. 选择一个圆（或直线）
2. 对任意点进行反演变换
3. 这会将一个多边形变成新的多边形
4. 重复此过程生成完整的镶嵌

---

## 🎨 创意应用

### 艺术创作
- 生成数学艺术作品
- 制作装饰图案
- 创建视觉设计素材

### 教学应用
- 演示双曲几何概念
- 制作数学教材插图
- 启发学生想象力

### 3D 打印
- 生成 STL 模型
- 制作数学教学模型
- 创作几何装饰品

### 数据可视化
- 投影复杂数据结构
- 创建非欧几里得图表

---

## 📞 支持

遇到问题？

1. 查看 README_STREAMLIT.md 获取更多信息
2. 检查终端输出是否有错误信息
3. 尝试重新启动应用
4. 清除浏览器缓存 (Ctrl+Shift+Delete)

---

## 📝 许可证

该项目基于原始的 Poincaré 项目开发。

---

**祝您使用愉快！🎉**

有任何建议或发现的 Bug，欢迎反馈！
