# 🚀 快速启动指南

## ⚡ 三步启动应用

### 方法 1: 双击启动（推荐）

在项目目录中双击以下文件之一：
- **`run.bat`** - Windows 命令提示符
- **`run.ps1`** - PowerShell

### 方法 2: 命令行启动

```bash
# 使用 PowerShell
cd "c:\Users\21140\Desktop\数学的天空网站\poincare"
python -m streamlit run app.py

# 或使用 CMD
cd c:\Users\21140\Desktop\数学的天空网站\poincare
python -m streamlit run app.py
```

### 方法 3: 使用启动脚本

```powershell
# PowerShell
.\run.ps1

# 或 CMD
run.bat
```

---

## 📱 应用界面

应用启动后，浏览器会自动打开 `http://localhost:8501`

左侧边栏提供三个功能选项：

### ✨ 功能 1: 基础镶嵌生成
- 生成双曲镶嵌图案
- 实时调整参数
- 预览效果
- 下载 STL 模型

### ✨ 功能 2: 文字投影
- 输入中文文字
- 投影到双曲空间
- 创建文字艺术
- 调整缩放和位置

### ✨ 功能 3: 图片处理
- 上传图片
- 边界检测
- 线条提取
- 双曲投影

---

## 🎮 使用示例

### 例 1: 生成六边形镶嵌

1. 启动应用 → 选择"基础镶嵌生成"
2. 在左侧选择 **镶嵌类型: hex**
3. 调整参数：
   - p = 7
   - q = 3
   - k = 6
4. 右侧实时预览
5. 点击"下载 STL 模型"

### 例 2: 创建文字艺术

1. 启动应用 → 选择"文字投影"
2. 输入文字：`你好` 或 `Poincaré`
3. 调整参数：
   - 缩放倍数 = 1.5
   - 垂直偏移 = 0.2
4. 查看投影效果

### 例 3: 处理图片

1. 启动应用 → 选择"图片处理"
2. 上传图片文件
3. 调整边界阈值
4. 查看边界检测结果

---

## 💻 系统要求

| 项目       | 要求                 |
| ---------- | -------------------- |
| **Python** | 3.8 或更高           |
| **内存**   | 至少 2GB RAM         |
| **磁盘**   | 至少 500MB 可用空间  |
| **浏览器** | 任何现代浏览器       |
| **网络**   | 首次启动需要下载依赖 |

---

## 🔧 依赖包

已自动安装的主要包：

```
✓ streamlit         - Web 框架
✓ matplotlib        - 绘图库
✓ numpy            - 数值计算
✓ pillow           - 图片处理
✓ opencv-python    - 图像处理
✓ trimesh          - 3D 模型处理
```

查看完整依赖：[requirements.txt](requirements.txt)

---

## ⏱️ 首次启动

首次启动可能比较慢，因为需要：
1. 编译应用代码
2. 加载所有库
3. 构建前端界面

**预计时间**: 30-60 秒

之后的重新加载会快得多（< 5 秒）

---

## 🎯 常见操作

### 调整参数后没反应？

Streamlit 会自动检测参数变化并重新运行。
查看右下角有"Rerun"提示时点击，或等待自动刷新。

### 如何保存结果？

1. **PNG 图像**: 使用浏览器截图 (F12) 或内置下载按钮
2. **STL 模型**: 点击"📥 下载 STL 模型"按钮

### 调整浏览器窗口大小

Streamlit 应用是响应式的，支持所有窗口大小。
建议宽屏显示器，以便同时查看参数和预览。

### 停止应用

按 `Ctrl+C` 停止服务器

---

## 🐛 故障排除

### 问题: 应用无法启动

**解决方案**:
```bash
# 重新安装依赖
pip install -r requirements.txt

# 检查 Python 版本
python --version  # 应为 3.8+

# 清除缓存后重试
streamlit run app.py --logger.level=debug
```

### 问题: 端口 8501 已被占用

**解决方案**:
```bash
# 使用其他端口
streamlit run app.py --server.port=8502
```

### 问题: 内存不足

**解决方案**:
- 降低 k 值（递归深度）
- 减小图片尺寸
- 关闭其他应用

### 问题: 文字投影错误

**解决方案**:
- 确保输入的是有效文字
- 检查系统是否安装了中文字体
- 尝试减少输入的文字数量

---

## 📚 更多信息

详细使用指南：[USAGE_GUIDE.md](USAGE_GUIDE.md)

完整项目文档：[README_STREAMLIT.md](README_STREAMLIT.md)

---

## 💡 提示

- 使用 **Firefox** 或 **Chrome** 获得最佳体验
- 建议将浏览器窗口**全屏**显示
- 调整参数时注意**实时预览**，无需每次点击按钮
- **导出前**先通过预览确认效果

---

**祝您使用愉快！** 🎉

有问题？查看 [USAGE_GUIDE.md](USAGE_GUIDE.md) 获取详细帮助。
